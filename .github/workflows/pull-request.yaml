name: Pull Request
on:
  pull_request:
    branches:
    - main

jobs:
  pull-request:
    name: Pull Request
    uses: gramLabs/.github/.github/workflows/pull-request-go.yaml@main
    secrets:
      ssh-private-key: ${{ secrets.key }}
